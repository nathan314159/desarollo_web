        // Función que muestra un div (caja informativa) según su ID
        function mostrar(id) {
            document.getElementById(id).style.display = 'block';
        }

        // Función que oculta un div (caja informativa) según su ID
        function cerrar(id) {
            document.getElementById(id).style.display = 'none';
        }